import { MarketingNavbarClient } from "@/components/marketing/marketing-navbar-client";

/** Static shell: authentication is resolved only in the browser. */
export function MarketingNavbar() {
  return <MarketingNavbarClient />;
}
